let age;
age = window.prompt(`what's your age?`)
if (age > 18){
    console.log("u can have access");
   age = Number.age;
}

else{
    console.log("u must be 18+ to enter the site");
    age = Number.age;
}

